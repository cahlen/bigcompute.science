---
import Base from '../layouts/Base.astro';
---

<Base title="Interactive" description="Live mathematical visualizations powered by Pretext">
  <h1>Interactive Mathematics</h1>
  <p class="page-intro">
    Live visualizations of our computational results. Text layout powered by
    <a href="https://github.com/chenglou/pretext">Pretext</a> —
    measuring and flowing text without DOM reflows.
  </p>

  <section class="distributed-section m-reveal">
    <div class="distributed-hero">
      <h2>Distributed Mathematics Research</h2>
      <p>Every Google Colab instance comes with a free NVIDIA T4 GPU. When you open one of these notebooks, your GPU auto-detects, the CUDA kernels compile for your hardware, and you're running experiments on open mathematical conjectures within a minute.</p>
      <p>This is large-scale distributed computation. Each Colab session that runs an experiment extends the verified frontier &mdash; a new digit set checked, a new density computed, a new character table archived. The results are downloadable and submittable as a PR. <strong>No math PhD required. No funding required. Just a browser.</strong></p>
    </div>
  </section>

  <section class="colab-section m-reveal">
    <div class="colab-card">
      <div class="colab-text">
        <h2>MCP Explorer + GPU Compute</h2>
        <p>Query 23 tools (arXiv, zbMATH, OEIS, LMFDB, Lean/Mathlib), explore datasets, <strong>then compile and run CUDA experiments on your free GPU</strong>. No API key needed for exploration; GPU compute is one click.</p>
      </div>
      <a href="https://colab.research.google.com/github/cahlen/bigcompute.science/blob/main/public/notebooks/bigcompute_mcp_explorer.ipynb"
         target="_blank" class="colab-btn">
        <img src="https://colab.research.google.com/assets/colab-badge.svg" alt="Open in Colab" />
        Open MCP Explorer
      </a>
    </div>
  </section>

  <section class="colab-section m-reveal">
    <div class="colab-card" style="border-color:rgba(232,196,122,0.3)">
      <div class="colab-text">
        <h2>Research Agent + GPU Compute</h2>
        <p>The full autonomous loop: <strong>compile CUDA kernels, run experiments, harvest results, AI peer review, fix issues</strong>. Auto-detects your GPU (T4/A100/L4/4090/H100). Free Gemini API key for reviews, or use OpenAI/Anthropic.</p>
      </div>
      <a href="https://colab.research.google.com/github/cahlen/bigcompute.science/blob/main/public/notebooks/bigcompute_research_agent.ipynb"
         target="_blank" class="colab-btn">
        <img src="https://colab.research.google.com/assets/colab-badge.svg" alt="Open in Colab" />
        Open Research Agent
      </a>
    </div>
  </section>

  <section class="gpu-zoo m-reveal">
    <h2>The GPU Zoo</h2>
    <p class="gpu-zoo-intro">Every GPU runs our CUDA kernels — from a free Colab T4 to the 8×B200 cluster. From a free Colab T4 to our 8×B200 cluster. Tap any card to expand.</p>

    <div class="gpu-zoo-grid" id="gpu-zoo">
      <details class="gpu-card gpu-tier-free" data-gpu="t4">
        <summary class="gpu-card-header">
          <span class="gpu-tier-badge gpu-tb-free">Free</span>
          <h3>Tesla T4</h3>
        </summary>
        <div class="gpu-card-specs">
          <div class="gpu-spec"><span class="gpu-spec-val">16 GB</span><span class="gpu-spec-key">VRAM</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">300 GB/s</span><span class="gpu-spec-key">Bandwidth</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">8.1</span><span class="gpu-spec-key">FP32 TFLOPS</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">70W</span><span class="gpu-spec-key">Power</span></div>
        </div>
        <div class="gpu-card-context">
          <p><strong>Turing</strong> architecture (2018) · 12nm · Colab free tier</p>
          <p class="gpu-can-do">Can verify Zaremba to 10^10 (1.25 GB bitset). Kronecker S₂₀ in 4 sec. Quick wins in seconds.</p>
          <p class="gpu-cant-do">Cannot: Zaremba 10^11+ (12.5 GB bitset), full S₄₀ Kronecker triple-sum.</p>
        </div>
      </details>

      <details class="gpu-card gpu-tier-consumer" data-gpu="rtx4060">
        <summary class="gpu-card-header">
          <span class="gpu-tier-badge gpu-tb-consumer">Budget</span>
          <h3>RTX 4060</h3>
        </summary>
        <div class="gpu-card-specs">
          <div class="gpu-spec"><span class="gpu-spec-val">8 GB</span><span class="gpu-spec-key">VRAM</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">272 GB/s</span><span class="gpu-spec-key">Bandwidth</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">15.1</span><span class="gpu-spec-key">FP32 TFLOPS</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">115W</span><span class="gpu-spec-key">Power</span></div>
        </div>
        <div class="gpu-card-context">
          <p><strong>Ada Lovelace</strong> architecture (2023) · 5nm · ~$300 street price</p>
          <p class="gpu-can-do">Quick wins instantly. Zaremba to 10^9 (0.12 GB). Kronecker S₂₀. Good starter GPU for math.</p>
          <p class="gpu-cant-do">Cannot: Zaremba 10^10+ (1.25 GB barely fits). Limited by 8 GB VRAM.</p>
        </div>
      </details>

      <details class="gpu-card gpu-tier-consumer" data-gpu="rtx4090">
        <summary class="gpu-card-header">
          <span class="gpu-tier-badge gpu-tb-consumer">Enthusiast</span>
          <h3>RTX 4090</h3>
        </summary>
        <div class="gpu-card-specs">
          <div class="gpu-spec"><span class="gpu-spec-val">24 GB</span><span class="gpu-spec-key">VRAM</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">1.0 TB/s</span><span class="gpu-spec-key">Bandwidth</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">82.6</span><span class="gpu-spec-key">FP32 TFLOPS</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">450W</span><span class="gpu-spec-key">Power</span></div>
        </div>
        <div class="gpu-card-context">
          <p><strong>Ada Lovelace</strong> architecture (2022) · 5nm · The gamer's beast (~$1,600)</p>
          <p class="gpu-can-do">Zaremba to 10^11 (12.5 GB fits in 24 GB). Kronecker S₃₀. Serious compute for the price.</p>
          <p class="gpu-cant-do">Cannot: multi-GPU NVLink. No HBM (GDDR6X latency). Single card only.</p>
        </div>
      </details>

      <details class="gpu-card gpu-tier-pro" data-gpu="a100">
        <summary class="gpu-card-header">
          <span class="gpu-tier-badge gpu-tb-pro">Pro</span>
          <h3>A100</h3>
        </summary>
        <div class="gpu-card-specs">
          <div class="gpu-spec"><span class="gpu-spec-val">40–80 GB</span><span class="gpu-spec-key">VRAM</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">2.0 TB/s</span><span class="gpu-spec-key">Bandwidth</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">19.5</span><span class="gpu-spec-key">FP32 TFLOPS</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">400W</span><span class="gpu-spec-key">Power</span></div>
        </div>
        <div class="gpu-card-context">
          <p><strong>Ampere</strong> architecture (2020) · 7nm · Colab Pro or cloud</p>
          <p class="gpu-can-do">Zaremba to 10^11 easily. Kronecker S₃₀ in minutes. Class numbers at serious scale.</p>
          <p class="gpu-cant-do">Cannot: 8-GPU NVLink workloads (single card).</p>
        </div>
      </details>

      <details class="gpu-card gpu-tier-consumer" data-gpu="rtx5090">
        <summary class="gpu-card-header">
          <span class="gpu-tier-badge gpu-tb-consumer">Consumer</span>
          <h3>RTX 5090</h3>
        </summary>
        <div class="gpu-card-specs">
          <div class="gpu-spec"><span class="gpu-spec-val">32 GB</span><span class="gpu-spec-key">VRAM</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">1.8 TB/s</span><span class="gpu-spec-key">Bandwidth</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">105</span><span class="gpu-spec-key">FP32 TFLOPS</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">575W</span><span class="gpu-spec-key">Power</span></div>
        </div>
        <div class="gpu-card-context">
          <p><strong>Blackwell</strong> architecture (2025) · 4nm · Our local dev machine</p>
          <p class="gpu-can-do">Hausdorff spectrum (all 2²⁰ subsets, 72 min). Faster than H100 on FP32. Zaremba to 10^10 in seconds.</p>
          <p class="gpu-cant-do">Cannot: multi-GPU NVLink. Limited to 32 GB (no S₄₀ full triple-sum).</p>
        </div>
      </details>

      <details class="gpu-card gpu-tier-datacenter" data-gpu="h100">
        <summary class="gpu-card-header">
          <span class="gpu-tier-badge gpu-tb-datacenter">Datacenter</span>
          <h3>H100 SXM</h3>
        </summary>
        <div class="gpu-card-specs">
          <div class="gpu-spec"><span class="gpu-spec-val">80 GB</span><span class="gpu-spec-key">VRAM</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">3.4 TB/s</span><span class="gpu-spec-key">Bandwidth</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">67</span><span class="gpu-spec-key">FP32 TFLOPS</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">700W</span><span class="gpu-spec-key">Power</span></div>
        </div>
        <div class="gpu-card-context">
          <p><strong>Hopper</strong> architecture (2022) · 4nm · Cloud providers</p>
          <p class="gpu-can-do">80 GB VRAM handles most large problems. NVLink for multi-GPU. The workhorse of AI datacenters.</p>
          <p class="gpu-cant-do">Cannot: match B200 bandwidth (3.4 vs 8 TB/s). No FP4 tensor cores.</p>
        </div>
      </details>

      <details class="gpu-card gpu-tier-beast" data-gpu="b200">
        <summary class="gpu-card-header">
          <span class="gpu-tier-badge gpu-tb-beast">Our Cluster</span>
          <h3>B200 SXM</h3>
        </summary>
        <div class="gpu-card-specs">
          <div class="gpu-spec"><span class="gpu-spec-val">192 GB</span><span class="gpu-spec-key">VRAM</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">8 TB/s</span><span class="gpu-spec-key">Bandwidth</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">75</span><span class="gpu-spec-key">FP32 TFLOPS</span></div>
          <div class="gpu-spec"><span class="gpu-spec-val">1000W</span><span class="gpu-spec-key">Power</span></div>
        </div>
        <div class="gpu-card-context">
          <p><strong>Blackwell</strong> architecture (2024) · 4nm · Our 8×B200 DGX cluster (1.43 TB total)</p>
          <p class="gpu-can-do">Everything. Zaremba 10^12 (122 GB bitset). Ramsey 4.4T extensions in 130 sec. Kronecker S₃₀ in 7 min. 8 GPUs linked via NVLink 5.</p>
          <p class="gpu-cant-do">Costs: ~$2M for the DGX system. That's what <a href="https://guerrillamathematics.com">the merch</a> funds.</p>
        </div>
      </details>
    </div>

    <div class="gpu-scale-bar m-reveal">
      <div class="gpu-scale-label">Relative VRAM</div>
      <div class="gpu-scale-track">
        <div class="gpu-scale-segment gpu-tb-free" style="width:1.04%"><span>T4</span></div>
        <div class="gpu-scale-segment gpu-tb-pro" style="width:5.2%"><span>A100</span></div>
        <div class="gpu-scale-segment gpu-tb-consumer" style="width:2.08%"><span>5090</span></div>
        <div class="gpu-scale-segment gpu-tb-datacenter" style="width:5.2%"><span>H100</span></div>
        <div class="gpu-scale-segment gpu-tb-beast" style="width:86.48%"><span>8×B200 = 1,536 GB</span></div>
      </div>
    </div>

    <small class="m-reveal" style="display:block;text-align:center;color:var(--text-secondary,#999);font-size:0.65rem;margin-top:0.5rem;">Kernels compile automatically for your hardware via <code>nvidia-smi --query-gpu=compute_cap</code></small>
  </section>

  <section class="manifesto" id="manifesto-section">
    <div class="manifesto-text">
      <h2 class="m-reveal">How This Site Was Built</h2>

      <p class="m-reveal">A human picked the problems. An AI wrote the CUDA kernels. The human decided which results mattered. The AI managed the data pipeline. Neither could have done this alone.</p>

      <p class="m-reveal">That's not a grand statement about the future of mathematics — it's just what happened here. One person with a GPU cluster and an AI assistant produced computational results across a dozen open problems in number theory and combinatorics. Some of those computations would have taken months of manual coding. With AI collaboration, they took hours.</p>

      <div class="m-stats m-reveal">
        <div class="m-stat"><span class="m-num" data-value="27">0</span><small>Zaremba exceptions<br/>with A=&#123;1,2,3&#125;</small></div>
        <div class="m-stat"><span class="m-num" data-value="26400000000" data-display="26.4B">0</span><small>Kronecker coefficients<br/>for S₃₀</small></div>
        <div class="m-stat"><span class="m-num" data-value="30000000000" data-display="30B">0</span><small>class numbers<br/>computed</small></div>
        <div class="m-stat"><span class="m-num" data-value="1048575">0</span><small>Hausdorff dimensions<br/>all subsets of &#123;1,...,20&#125;</small></div>
      </div>

      <p class="m-reveal">These are computational results, not proofs. None of this work has been peer-reviewed. The code is open, the data is open, and the reproduction commands are on every page — so anyone can check our work. That's the point. We're not claiming authority. We're publishing the computations and inviting scrutiny.</p>

      <p class="m-reveal">The AI didn't have mathematical insight. It had speed. It could write a CUDA kernel in minutes, debug it against test cases, optimize memory access patterns, and manage uploads to Hugging Face — all tasks that are tedious for a human but trivial for an AI. The human decided <em>what</em> to compute and <em>why</em> it mattered. The AI decided <em>how</em> to compute it efficiently.</p>

      <div class="m-pullquote m-reveal"><span>The bottleneck in computational mathematics has always been engineering, not ideas.</span></div>

      <p class="m-reveal">There are thousands of open conjectures where the next step is "compute this for larger values and see what happens." Most of those computations never get done because writing the GPU kernel, setting up the data pipeline, and publishing the results is more work than most researchers want to do for a speculative computation. AI collaboration removes that friction. The ideas were always there. Now they can be tested.</p>

      <p class="m-reveal">This site is an experiment in that process. Some of the results will turn out to be interesting. Some won't survive peer review. All of the data is here for you to use.</p>

      <p class="manifesto-cite m-reveal">— Cahlen Humphreys, 2026. Built with Claude.</p>
    </div>
  </section>

  <script is:inline>
  // Scroll-triggered reveals + number counter animation
  (function() {
    var reveals = document.querySelectorAll('.m-reveal');
    var nums = document.querySelectorAll('.m-num[data-value]');
    var counted = new Set();

    function onScroll() {
      var vh = window.innerHeight;
      for (var i = 0; i < reveals.length; i++) {
        var el = reveals[i];
        var top = el.getBoundingClientRect().top;
        if (top < vh * 0.85) el.classList.add('visible');
      }
      // Animate numbers when visible
      for (var j = 0; j < nums.length; j++) {
        var n = nums[j];
        if (counted.has(n)) continue;
        if (n.getBoundingClientRect().top < vh * 0.85) {
          counted.add(n);
          animateNum(n);
        }
      }
    }

    function animateNum(el) {
      var target = parseInt(el.getAttribute('data-value'));
      var display = el.getAttribute('data-display');
      var duration = 1200;
      var start = performance.now();
      function step(now) {
        var t = Math.min((now - start) / duration, 1);
        t = 1 - Math.pow(1 - t, 3); // ease out cubic
        var current = Math.round(target * t);
        if (display) {
          // Format as "26.4B" style
          var ratio = t;
          el.textContent = display.replace(/[\d.]+/, function(m) {
            return (parseFloat(m) * ratio).toFixed(m.includes('.') ? 1 : 0);
          });
        } else {
          el.textContent = current.toLocaleString();
        }
        if (t < 1) requestAnimationFrame(step);
      }
      requestAnimationFrame(step);
    }

    window.addEventListener('scroll', onScroll, { passive: true });
    onScroll(); // initial check
  })();
  </script>

  <section class="interactive-section">
    <h2>Zaremba Witness Distribution</h2>
    <p>
      Drag the slider to explore how the witness ratio α(d)/d concentrates
      around 0.171 as d grows. Each bubble is a value of d, sized by its
      CF length, colored by max partial quotient.
      <a href="/findings/zaremba-witness-golden-ratio/">Finding →</a>
      <a href="/experiments/zaremba-conjecture-verification/">Experiment →</a>
    </p>
    <div class="viz-container" id="witness-viz">
      <canvas id="witness-canvas"></canvas>
      <div class="viz-controls">
        <label>
          <span>Max d:</span>
          <input type="range" id="witness-max-d" min="100" max="10000" value="1000" step="100">
          <span id="witness-d-label" class="mono">1000</span>
        </label>
      </div>
    </div>
  </section>

  <section class="interactive-section">
    <h2>Spectral Gap Landscape</h2>
    <p>
      The congruence spectral gaps σ<sub>m</sub> for all 1,214 square-free moduli
      up to m=1999. Hover over any point to see the modulus, factorization,
      and gap value. The red dashed line marks the Bourgain-Kontorovich threshold.
      <a href="/findings/zaremba-spectral-gaps-uniform/">Finding →</a>
      <a href="/experiments/zaremba-transfer-operator/">Experiment →</a>
    </p>
    <div class="viz-container" id="spectral-viz">
      <canvas id="spectral-canvas"></canvas>
      <div id="spectral-tooltip" class="tooltip"></div>
    </div>
  </section>

  <section class="interactive-section">
    <h2>CF Tree Explorer</h2>
    <p>
      The continued fraction tree with partial quotients in &#123;1, &hellip;, 5&#125;.
      Each path from root to leaf gives a Zaremba denominator. Watch the
      tree grow and see how quickly it covers the integers.
      <a href="/findings/zaremba-conjecture-proved/">Finding →</a>
    </p>
    <div class="viz-container" id="tree-viz">
      <canvas id="tree-canvas"></canvas>
      <div class="viz-controls">
        <label>
          <span>Depth:</span>
          <input type="range" id="tree-depth" min="1" max="9" value="5">
          <span id="tree-depth-label" class="mono">6</span>
        </label>
        <button id="tree-animate" class="viz-btn">Animate Growth</button>
      </div>
    </div>
  </section>

  <section class="interactive-section">
    <h2>Zaremba Density Phase Transition</h2>
    <p>
      For digit set A, the Zaremba density measures what fraction of integers d have a
      coprime a/d with all CF partial quotients in A. A sharp phase transition occurs
      between A=&#123;1,2&#125; (58%) and A=&#123;1,2,3&#125; (99.997%). The transition is controlled
      by the Hausdorff dimension crossing 1/2.
      <a href="/findings/zaremba-density-phase-transition/">Finding →</a>
    </p>
    <div class="viz-container" id="density-viz">
      <canvas id="density-canvas"></canvas>
    </div>
  </section>

  <section class="interactive-section">
    <h2>Class Number Distribution</h2>
    <p>
      Distribution of class numbers h(d) for 2.74 billion real quadratic fields
      Q(√d) with d&nbsp;∈&nbsp;[10<sup>9</sup>,&nbsp;10<sup>10</sup>). Dominated by powers of 2
      (genus theory). The Cohen-Lenstra prediction of 75% with h=1 is far from realized
      at this scale — convergence is non-monotone.
      <a href="/findings/class-number-convergence/">Finding →</a>
      <a href="/experiments/class-numbers-real-quadratic/">Experiment →</a>
    </p>
    <div class="viz-container" id="classnum-viz">
      <canvas id="classnum-canvas"></canvas>
    </div>
  </section>

  <section class="interactive-section">
    <h2>Hausdorff Dimension Spectrum</h2>
    <p>
      Hausdorff dimension dim<sub>H</sub>(E<sub>A</sub>) for every subset
      A&nbsp;⊆&nbsp;&#123;1,…,10&#125;, grouped by cardinality |A|. Each dot is one of
      the 1,023 nonempty subsets. Adding digit 1 has the largest effect on
      dimension — 5 digits including 1 beat 9 digits without it.
      <a href="/findings/hausdorff-digit-one-dominance/">Finding →</a>
      <a href="/experiments/hausdorff-dimension-spectrum/">Experiment →</a>
    </p>
    <div class="viz-container" id="hausdorff-viz">
      <canvas id="hausdorff-canvas"></canvas>
    </div>
  </section>

  <section class="rosetta">
    <h2>CUDA Kernels: A Rosetta Stone</h2>
    <p class="rosetta-intro">
      The same computation explained three ways. The mathematician and computer scientist think they speak different languages — but they're describing the same structure. The third column shows what actually happened: a human directing an AI agent. That's how the kernel got built. Not by hand-coding CUDA. By conversation.
    </p>

    <div class="rosetta-example">
      <h3>The Problem: Is this number "Zaremba-good"?</h3>
      <p class="rosetta-problem">Given an integer d, does there exist a coprime a/d whose continued fraction has all partial quotients at most 5?</p>

      <div class="rosetta-columns">
        <div class="rosetta-col rosetta-math">
          <h4>For the Mathematician</h4>

          <div class="rosetta-step">
            <span class="rosetta-label">The object</span>
            <p>We study the semigroup generated by matrices
              M(a) = ((a,1),(1,0)) for a = 1,...,5 acting on pairs (p,q).
              Each product gives a convergent p/q of a bounded CF.</p>
          </div>

          <div class="rosetta-step">
            <span class="rosetta-label">The recursion</span>
            <p>The convergent recurrence: p_new = a*p + p_prev, q_new = a*q + q_prev.
              This is just matrix multiplication in SL(2,Z). Each step extends the CF by one partial quotient.</p>
          </div>

          <div class="rosetta-step">
            <span class="rosetta-label">The search</span>
            <p>Enumerate the tree of all finite products. At each node, the denominator q is a "covered" integer.
              If every integer up to N appears as some q, Zaremba's conjecture holds to N.</p>
          </div>

          <div class="rosetta-step">
            <span class="rosetta-label">The insight</span>
            <p>The tree grows exponentially (branching factor 5), but most branches produce large q quickly.
              The density of covered integers depends on the Hausdorff dimension of the underlying Cantor set E_5 — specifically, on whether 2*delta > 1.</p>
          </div>

          <div class="rosetta-step rosetta-bridge">
            <span class="rosetta-label">What AI does</span>
            <p>Translates this tree enumeration into a GPU kernel where each thread explores an independent subtree. The mathematician says "enumerate the semigroup." The AI writes the parallel DFS.</p>
          </div>
        </div>

        <div class="rosetta-col rosetta-cs">
          <h4>For the Computer Scientist</h4>

          <div class="rosetta-step">
            <span class="rosetta-label">The data structure</span>
            <p>A 5-ary tree where each node stores a state (p_prev, p, q_prev, q).
              Children are generated by multiplying the state by 5 different small integers.
              We need to mark which values of q appear anywhere in the tree.</p>
          </div>

          <div class="rosetta-step">
            <span class="rosetta-label">The recurrence</span>
            <p>State transition: (pp, p, qp, q) + digit a gives (p, a*p+pp, q, a*q+qp).
              Four integer multiplies and two adds per step. The tree terminates when q exceeds a threshold N.</p>
          </div>

          <div class="rosetta-step">
            <span class="rosetta-label">The parallelism</span>
            <p>Enumerate the tree to depth k on CPU, producing ~5^k independent subtrees.
              Each GPU thread takes one subtree and does iterative DFS, marking q values in a shared bitset via atomicOr.</p>
          </div>

          <div class="rosetta-step">
            <span class="rosetta-label">The optimization</span>
            <p>The tree is unbalanced — branches with small digits (a=1) go deepest.
              Prefix depth k should be tuned so each GPU thread gets roughly equal work.
              Memory: one bit per integer up to N. For N=10^9, that's 125 MB — fits in any GPU.</p>
          </div>

          <div class="rosetta-step rosetta-bridge">
            <span class="rosetta-label">What AI does</span>
            <p>Understands the mathematical structure well enough to choose the right parallelization strategy.
              The CS person says "parallelize the tree search." The AI knows that the branching factor and Hausdorff dimension determine the optimal prefix depth.</p>
          </div>
        </div>

        <div class="rosetta-col rosetta-agent">
          <h4>What Actually Happened</h4>
          <p class="rosetta-agent-note">These are real quotes from the session that built this kernel. The human caught a validation bug (76 vs 27) and pushed back on single-GPU usage. The agent debugged and fixed. This is how the code on this site was actually written — not by hand, but through conversation.</p>

          <div class="rosetta-step rosetta-chat">
            <span class="rosetta-label rosetta-human-label">Human</span>
            <p class="rosetta-human">The CPU density code has been running 12 hours. A CUDA kernel would finish in minutes.</p>
          </div>

          <div class="rosetta-step rosetta-chat">
            <span class="rosetta-label rosetta-agent-label">Agent</span>
            <p class="rosetta-ai">The CF tree enumeration is embarrassingly parallel. Each branch is independent. I'll generate prefixes on CPU to depth 8, giving ~6500 GPU threads, then each thread does DFS on its subtree.</p>
          </div>

          <div class="rosetta-step rosetta-chat">
            <span class="rosetta-label rosetta-human-label">Human</span>
            <p class="rosetta-human">Why are we only using 1 GPU? We have 8.</p>
          </div>

          <div class="rosetta-step rosetta-chat">
            <span class="rosetta-label rosetta-agent-label">Agent</span>
            <p class="rosetta-ai">Each digit set at each range is independent. Let me launch all 8 — different digit sets in parallel.</p>
          </div>

          <div class="rosetta-step rosetta-chat">
            <span class="rosetta-label rosetta-human-label">Human</span>
            <p class="rosetta-human">Validation says 76 uncovered instead of 27. Something's wrong.</p>
          </div>

          <div class="rosetta-step rosetta-chat">
            <span class="rosetta-label rosetta-agent-label">Agent</span>
            <p class="rosetta-ai">The prefix depth skips CFs shorter than 8. Depths 4-7 aren't being marked. I need the CPU to handle all depths below PREFIX_DEPTH, then GPU handles the rest.</p>
          </div>

          <div class="rosetta-step rosetta-chat">
            <span class="rosetta-label rosetta-human-label">Human</span>
            <p class="rosetta-human">27 exceptions confirmed. Same 27 as at 10^6. Zero new ones to 10^{10}.</p>
          </div>

          <div class="rosetta-step rosetta-bridge">
            <span class="rosetta-label">The point</span>
            <p>The human never wrote a line of CUDA. The human never debugged a thread index. The human said what the computation should do and whether the answer was right. The agent handled the engineering. That's how a mathematician who understands the problem but doesn't want to spend weeks on GPU plumbing gets a result in an afternoon.</p>
          </div>
        </div>
      </div>

      <div class="rosetta-convergence">
        <p><strong>Notice:</strong> the mathematician's "semigroup enumeration" and the computer scientist's "parallel tree search" are the same algorithm.
          The "Hausdorff dimension" and the "tree balance factor" are the same number.
          The "convergent recurrence" and the "state transition" are the same four lines of code.</p>
        <p>The fields use different words for identical structures. AI doesn't care about the vocabulary — it sees the computation underneath. That's why it can translate between them, and why a mathematician with an AI assistant can write GPU kernels, and a systems programmer with an AI assistant can explore number theory.</p>
      </div>
    </div>

    <div class="rosetta-example">
      <h3>The Actual Kernel</h3>
      <p class="rosetta-problem">Here's the CUDA code. 30 lines. Both audiences can now read it.</p>
      <pre class="rosetta-code">__global__ void enumerate_subtrees(
    uint64 *prefixes, int num_prefixes,
    int *digits, int num_digits,
    uint8_t *bitset, uint64 max_d)
&#123;
    int tid = blockIdx.x * blockDim.x + threadIdx.x;
    if (tid >= num_prefixes) return;

    // Load this thread's starting state (from CPU-generated prefix)
    uint64 pp = prefixes[tid*4], p = prefixes[tid*4+1];
    uint64 qp = prefixes[tid*4+2], q = prefixes[tid*4+3];

    // Mark the denominator
    atomicOr(&amp;bitset[q >> 3], 1 &lt;&lt; (q &amp; 7));

    // DFS: iterative stack, each thread independent
    struct &#123; uint64 pp,p,qp,q; &#125; stack[200];
    int sp = 0;

    for (int i = num_digits-1; i >= 0; i--) &#123;
        uint64 qn = digits[i]*q + qp;
        if (qn > max_d) continue;
        stack[sp++] = &#123;p, digits[i]*p+pp, q, qn&#125;;
    &#125;

    while (sp > 0) &#123;
        auto s = stack[--sp];
        atomicOr(&amp;bitset[s.q >> 3], 1 &lt;&lt; (s.q &amp; 7));
        for (int i = num_digits-1; i >= 0; i--) &#123;
            uint64 qn = digits[i]*s.q + s.qp;
            if (qn > max_d) continue;
            if (sp &lt; 199)
                stack[sp++] = &#123;s.p, digits[i]*s.p+s.pp, s.q, qn&#125;;
        &#125;
    &#125;
&#125;</pre>
    </div>
  </section>

  <style>
    .page-intro {
      color: var(--muted);
      font-style: italic;
      margin-bottom: 3rem;
    }
    .page-intro a { color: var(--accent); text-decoration: none; border-bottom: 1px dotted var(--accent); }

    /* Distributed research hero */
    .distributed-section { margin-bottom: 2rem; }
    .distributed-hero {
      text-align: center;
      max-width: 700px;
      margin: 0 auto;
      padding: 2rem 0;
    }
    .distributed-hero h2 {
      font-family: var(--font-serif, 'STIX Two Text', Georgia, serif);
      font-size: 1.6rem;
      letter-spacing: -0.03em;
      margin-bottom: 1rem;
      color: #e8c47a;
    }
    .distributed-hero p {
      font-size: 0.92rem;
      color: var(--text-secondary, #999);
      line-height: 1.7;
      margin-bottom: 0.8rem;
    }

    /* GPU compatibility grid */
    .gpu-compat { margin-bottom: 3rem; }
    .gpu-compat-inner {
      text-align: center;
      max-width: 700px;
      margin: 0 auto;
    }
    .gpu-compat-inner h3 {
      font-size: 0.85rem;
      color: var(--text-secondary, #999);
      margin-bottom: 0.8rem;
      font-weight: 400;
    }
    .gpu-grid {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 0.5rem;
      margin-bottom: 0.6rem;
    }
    .gpu-chip {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 0.4rem 0.8rem;
      background: color-mix(in srgb, var(--surface, #1a1a1a) 80%, transparent);
      border: 1px solid color-mix(in srgb, var(--border, #333) 40%, transparent);
      border-radius: 6px;
      min-width: 5rem;
    }
    .gpu-chip:hover { border-color: rgba(232,196,122,0.3); }
    .gpu-name {
      font-family: var(--font-mono, 'JetBrains Mono', monospace);
      font-size: 0.78rem;
      font-weight: 700;
      color: #e8c47a;
    }
    .gpu-chip small {
      font-size: 0.58rem;
      color: var(--text-secondary, #999);
      opacity: 0.6;
    }
    .gpu-compat-inner > small {
      font-size: 0.65rem;
      color: var(--text-secondary, #999);
      opacity: 0.5;
    }
    .gpu-compat-inner code {
      font-size: 0.62rem;
      background: rgba(232,196,122,0.08);
      padding: 0.1rem 0.3rem;
      border-radius: 3px;
    }

    /* GPU Zoo */
    .gpu-zoo { margin-bottom: 3rem; max-width: 960px; margin-left: auto; margin-right: auto; }
    .gpu-zoo h2 {
      font-family: var(--font-serif, 'STIX Two Text', Georgia, serif);
      font-size: 1.5rem;
      text-align: center;
      letter-spacing: -0.03em;
      margin-bottom: 0.5rem;
    }
    .gpu-zoo-intro {
      text-align: center;
      font-size: 0.85rem;
      color: var(--text-secondary, #999);
      margin-bottom: 1.5rem;
    }
    .gpu-zoo-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
      gap: 1rem;
    }
    .gpu-card {
      background: color-mix(in srgb, var(--surface, #1a1a1a) 60%, transparent);
      border: 2px solid color-mix(in srgb, var(--border, #333) 40%, transparent);
      padding: 1.2rem;
      transition: border-color 0.2s, transform 0.2s;
      cursor: default;
    }
    .gpu-card:hover { transform: translateY(-2px); cursor: pointer; }
    .gpu-card summary { list-style: none; cursor: pointer; }
    .gpu-card summary::-webkit-details-marker { display: none; }
    .gpu-card summary::marker { display: none; content: ""; }
    .gpu-card .gpu-card-specs,
    .gpu-card .gpu-card-context { display: none; }
    .gpu-card[open] .gpu-card-specs { display: grid; }
    .gpu-card[open] .gpu-card-context { display: block; }
    .gpu-tier-free:hover { border-color: #22c55e; }
    .gpu-tier-pro:hover { border-color: #3b82f6; }
    .gpu-tier-consumer:hover { border-color: #e8c47a; }
    .gpu-tier-datacenter:hover { border-color: #a855f7; }
    .gpu-tier-beast:hover { border-color: #ef4444; }

    .gpu-card-header {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-bottom: 0.8rem;
    }
    .gpu-card-header h3 {
      font-family: var(--font-mono, 'JetBrains Mono', monospace);
      font-size: 1rem;
      font-weight: 800;
      color: var(--text-primary, #e5e5e5);
      margin: 0;
    }
    .gpu-tier-badge {
      font-size: 0.5rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      padding: 0.15rem 0.4rem;
      border-radius: 2px;
    }
    .gpu-tb-free { background: rgba(34,197,94,0.15); color: #22c55e; }
    .gpu-tb-pro { background: rgba(59,130,246,0.15); color: #3b82f6; }
    .gpu-tb-consumer { background: rgba(232,196,122,0.15); color: #e8c47a; }
    .gpu-tb-datacenter { background: rgba(168,85,247,0.15); color: #a855f7; }
    .gpu-tb-beast { background: rgba(239,68,68,0.15); color: #ef4444; }

    .gpu-card-specs {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 0.4rem;
      margin-bottom: 0.8rem;
      padding-bottom: 0.8rem;
      border-bottom: 1px solid color-mix(in srgb, var(--border, #333) 30%, transparent);
    }
    .gpu-spec { display: flex; flex-direction: column; }
    .gpu-spec-val {
      font-family: var(--font-mono, monospace);
      font-size: 0.85rem;
      font-weight: 700;
      color: var(--text-primary, #e5e5e5);
    }
    .gpu-spec-key {
      font-size: 0.55rem;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--text-secondary, #999);
    }

    .gpu-card-context p {
      font-size: 0.72rem;
      color: var(--text-secondary, #999);
      line-height: 1.4;
      margin: 0 0 0.3rem;
    }
    .gpu-card-context p:first-child { color: var(--text-primary, #e5e5e5); opacity: 0.7; }
    .gpu-can-do { color: #22c55e !important; }
    .gpu-cant-do { color: #ef4444 !important; opacity: 0.6; }
    .gpu-cant-do a { color: #e8c47a; }

    /* Scale bar */
    .gpu-scale-bar { margin: 1.5rem auto; max-width: 700px; }
    .gpu-scale-label {
      font-size: 0.6rem;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: var(--text-secondary, #999);
      margin-bottom: 0.4rem;
      text-align: center;
    }
    .gpu-scale-track {
      display: flex;
      height: 24px;
      background: rgba(255,255,255,0.03);
      overflow: hidden;
    }
    .gpu-scale-segment {
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.5rem;
      font-weight: 700;
      color: #0a0a0a;
      white-space: nowrap;
      overflow: hidden;
    }
    .gpu-scale-segment.gpu-tb-free { background: #22c55e; }
    .gpu-scale-segment.gpu-tb-pro { background: #3b82f6; }
    .gpu-scale-segment.gpu-tb-consumer { background: #e8c47a; }
    .gpu-scale-segment.gpu-tb-datacenter { background: #a855f7; }
    .gpu-scale-segment.gpu-tb-beast { background: #ef4444; }
    .gpu-scale-segment span { padding: 0 0.3rem; }

    @media (max-width: 600px) {
      .gpu-zoo-grid { grid-template-columns: 1fr; }
    }

    .colab-section { margin-bottom: 3rem; }
    .colab-card {
      background: var(--surface);
      border: 1px solid var(--rule);
      border-left: 3px solid #e8c47a;
      border-radius: 2px 8px 8px 2px;
      padding: 1.5rem 2rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 2rem;
      flex-wrap: wrap;
    }
    .colab-card h2 {
      font-family: var(--font-serif);
      font-size: 1.2rem;
      margin: 0 0 0.3rem;
      letter-spacing: -0.02em;
    }
    .colab-card p {
      font-size: 0.85rem;
      color: var(--ink-secondary);
      margin: 0;
      line-height: 1.5;
      max-width: 500px;
    }
    .colab-btn {
      display: flex;
      align-items: center;
      gap: 0.6rem;
      padding: 0.5rem 1rem;
      background: rgba(232,196,122,0.08);
      border: 1px solid rgba(232,196,122,0.3);
      border-radius: 6px;
      color: #e8c47a;
      text-decoration: none;
      font-family: var(--font-sans);
      font-size: 0.85rem;
      font-weight: 600;
      transition: all 0.15s;
      white-space: nowrap;
    }
    .colab-btn:hover { background: rgba(232,196,122,0.15); border-color: #e8c47a; }
    .colab-btn img { height: 20px; }
    @media (max-width: 700px) {
      .colab-card { flex-direction: column; align-items: flex-start; }
    }

    .manifesto {
      margin-bottom: 4rem;
      border: 1px solid var(--rule);
      border-radius: 6px;
      background: var(--surface);
      overflow: hidden;
    }
    .manifesto-text {
      padding: 3rem 3.5rem;
      max-width: 720px;
      margin: 0 auto;
    }
    .manifesto h2 {
      font-family: var(--font-serif);
      font-size: 1.6rem;
      margin-bottom: 2rem;
      color: var(--ink);
      letter-spacing: -0.02em;
      text-align: center;
    }
    .manifesto p {
      color: var(--ink-secondary);
      margin-bottom: 1.4rem;
      font-size: 0.95rem;
      line-height: 1.8;
    }
    .manifesto strong {
      color: #e8c47a;
    }
    .manifesto-cite {
      margin-top: 2.5rem;
      font-style: italic;
      color: var(--muted);
      font-size: 0.85rem;
      text-align: center;
    }

    /* Scroll reveal */
    .m-reveal {
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 0.7s ease, transform 0.7s ease;
    }
    .m-reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }

    /* Stagger children */
    .m-reveal:nth-child(2) { transition-delay: 0.05s; }
    .m-reveal:nth-child(3) { transition-delay: 0.1s; }
    .m-reveal:nth-child(4) { transition-delay: 0.15s; }

    /* Pull quotes */
    .m-pullquote {
      margin: 2.5rem 0;
      padding: 0;
      text-align: center;
    }
    .m-pullquote span {
      font-family: var(--font-serif);
      font-size: 1.35rem;
      font-style: italic;
      color: #e8c47a;
      letter-spacing: -0.01em;
      position: relative;
      display: inline-block;
      padding: 0.5rem 0;
    }
    .m-pullquote span::before,
    .m-pullquote span::after {
      content: '';
      display: block;
      width: 40px;
      height: 1px;
      background: rgba(232,196,122,0.3);
      margin: 0.8rem auto;
    }

    /* Glowing highlight */
    .m-glow {
      color: #e8c47a;
      text-shadow: 0 0 20px rgba(232,196,122,0.15);
    }

    /* Stats counter row */
    .m-stats {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 1.2rem;
      margin: 2.5rem 0;
      padding: 1.5rem 0;
      border-top: 1px solid rgba(232,196,122,0.1);
      border-bottom: 1px solid rgba(232,196,122,0.1);
    }
    .m-stat {
      text-align: center;
    }
    .m-stat .m-num {
      display: block;
      font-family: var(--font-mono);
      font-size: 1.6rem;
      color: #e8c47a;
      font-weight: bold;
      margin-bottom: 0.4rem;
    }
    .m-stat small {
      font-family: var(--font-sans);
      font-size: 0.72rem;
      color: var(--muted);
      line-height: 1.4;
      display: block;
    }

    @media (max-width: 700px) {
      .manifesto-text { padding: 2rem 1.5rem; }
      .m-stats { grid-template-columns: repeat(2, 1fr); }
      .m-pullquote span { font-size: 1.1rem; }
    }

    .interactive-section {
      margin-bottom: 4rem;
    }
    .interactive-section h2 {
      font-family: var(--font-serif);
      font-size: 1.3rem;
      margin-bottom: 0.75rem;
      color: var(--ink);
    }
    .interactive-section p {
      color: var(--ink-secondary);
      margin-bottom: 1.5rem;
      font-size: 0.92rem;
    }

    .viz-container {
      background: var(--surface);
      border: 1px solid var(--rule);
      border-radius: 6px;
      padding: 1.5rem;
      position: relative;
    }

    canvas {
      width: 100%;
      height: auto;
      display: block;
      border-radius: 4px;
    }

    .viz-controls {
      margin-top: 1rem;
      display: flex;
      gap: 1.5rem;
      align-items: center;
      flex-wrap: wrap;
    }

    .viz-controls label {
      display: flex;
      align-items: center;
      gap: 0.6rem;
      font-family: var(--font-sans);
      font-size: 0.8rem;
      color: var(--muted);
    }

    .viz-controls input[type="range"] {
      width: 200px;
      accent-color: var(--accent);
    }

    .mono { font-family: var(--font-mono); color: var(--accent); font-size: 0.82rem; }

    .viz-btn {
      font-family: var(--font-mono);
      font-size: 0.78rem;
      background: transparent;
      color: var(--accent);
      border: 1px solid color-mix(in srgb, var(--accent) 30%, transparent);
      padding: 0.35rem 0.8rem;
      border-radius: 4px;
      cursor: pointer;
      transition: background 160ms ease;
    }
    .viz-btn:hover { background: color-mix(in srgb, var(--accent) 8%, transparent); }

    .tooltip {
      display: none;
      position: absolute;
      background: var(--panel);
      border: 1px solid var(--rule);
      border-radius: 4px;
      padding: 0.5rem 0.75rem;
      font-family: var(--font-mono);
      font-size: 0.75rem;
      color: var(--ink);
      pointer-events: none;
      z-index: 10;
      white-space: nowrap;
    }
  </style>

  <div id="load-interactive"></div>
</Base>
